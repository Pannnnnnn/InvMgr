export default function Home() {
  return (
    <main style={{ fontFamily: 'system-ui', padding: 32 }}>
      <h1>FactoryLens backend</h1>
      <p>API-only service. See /api/checkout, /api/items, /api/transactions.</p>
    </main>
  );
}
